# list of file with all r w x permissions

#!/bin/bash
echo "List of files with read, write, and execute permissions:"

for file in *; do
    if [ -f "$file" ] && [ -r "$file" ] && [ -w "$file" ] && [ -x "$file" ]; then
        echo "$file"
    fi
done
